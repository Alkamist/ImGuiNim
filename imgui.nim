import
  imgui/[build, flags, types,
         functions, methods,
         constructors, destructors]

export
  build, flags, types,
  functions, methods,
  constructors, destructors

# {.emit: """/*INCLUDESECTION*/
# #include "imgui.h"
# #include "imgui_internal.h"
# """.}